version: '3.8'

services:
  frontend:
    image: colaborapp-frontend:latest
    container_name: colaborapp-frontend
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile